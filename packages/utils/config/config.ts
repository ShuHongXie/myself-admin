import type { Config } from './types'

export const config: Config = {
  theme: {
    colorPrimary: '#954df5'
  },
  sidebar: {
    collapse: false
  },
  app: {
    locale: 'zh-CN',
    name: 'Myself Admin'
  }
}
